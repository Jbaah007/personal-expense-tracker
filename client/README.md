# Personal Expense Tracker Client

This Vite app powers the frontend for the personal expense tracker.

## Available scripts
- npm run dev — start the Vite development server
- npm run build — create a production build
- npm run test — run the frontend tests

## Notes
The app includes expense entry, filtering, sorting, budgeting, and local persistence through the backend API.
